[project]
name = "lexplain"
version = "1.0.0"
description = "Multi-agent legal reasoning pipeline"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0",
    "rank-bm25>=0.2.2",
    "nltk>=3.8",
    "flask",
    "python-dotenv",
]

[project.scripts]
lexplain = "cli:main"
